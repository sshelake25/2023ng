import { GlobalErrorHandar } from './global-error-handar';

describe('GlobalErrorHandar', () => {
  it('should create an instance', () => {
    expect(new GlobalErrorHandar()).toBeTruthy();
  });
});
