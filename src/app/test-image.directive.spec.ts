import { TestImageDirective } from './test-image.directive';

describe('TestImageDirective', () => {
  it('should create an instance', () => {
    const directive = new TestImageDirective();
    expect(directive).toBeTruthy();
  });
});
